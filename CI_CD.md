# CI/CD Configuration Guide

## 📋 Обзор

Проект настроен с CI/CD пайплайнами для автоматической сборки, тестирования и развертывания Docker образов.

## 🔧 Поддерживаемые платформы

### 1. GitHub Actions
- **Файл:** `.github/workflows/ci.yml` - CI пайплайн
- **Файл:** `.github/workflows/deploy.yml` - CD пайплайн для продакшена

### 2. GitLab CI
- **Файл:** `.gitlab-ci.yml` - Полный CI/CD пайплайн для GitLab

## 🚀 Настройка GitHub Actions

### Шаг 1: Создание секретов в GitHub

Перейдите в Settings → Secrets and variables → Actions и добавьте:

1. **DOCKER_USERNAME** - Ваш Docker Hub username
2. **DOCKER_PASSWORD** - Ваш Docker Hub password или access token

### Шаг 2: Активация пайплайнов

Пайплайны автоматически запускаются при:
- Push в ветки `main`, `master`, `develop`
- Pull Request в эти ветки
- Создание тега `v*` (например, `v1.0.0`)

### Что делает CI пайплайн:

1. **Сборка образов** - Собирает все 5 Docker образов
2. **Тестирование** - Запускает docker-compose тесты
3. **Линтинг** - Проверяет Python код на ошибки
4. **Сканирование безопасности** - Проверяет уязвимости через Trivy

### Что делает Deploy пайплайн:

1. **Сборка и публикация** - Собирает и пушит образы в Docker Hub
2. **Версионирование** - Создает теги на основе версии или commit SHA
3. **Релизы** - Автоматически создает GitHub Release при создании тега

## 🐳 Настройка GitLab CI

### Шаг 1: Настройка переменных

В GitLab перейдите в Settings → CI/CD → Variables и добавьте:
- `CI_REGISTRY` - URL вашего GitLab Container Registry
- `CI_REGISTRY_USER` - GitLab username
- `CI_REGISTRY_PASSWORD` - GitLab password или token

### Шаг 2: Использование

GitLab CI автоматически:
- Собирает образы при push в `main`, `master`, `develop`
- Тестирует сервисы
- Публикует образы в GitLab Container Registry
- Предлагает ручное развертывание в продакшен

## 📦 Локальное тестирование

### Запуск тестов локально:

```bash
# Использование docker-compose для тестирования
docker-compose -f docker-compose.test.yml up --build --abort-on-container-exit
```

### Проверка образов:

```bash
# Список собранных образов
docker images | grep learning-platform

# Тестирование конкретного сервиса
docker run -p 5001:5001 learning-platform/auth-service:latest
```

## 🔍 Мониторинг пайплайнов

### GitHub Actions:
- Перейдите в вкладку "Actions" в вашем репозитории
- Просматривайте статус каждого запуска
- Проверяйте логи для отладки

### GitLab CI:
- Перейдите в CI/CD → Pipelines
- Просматривайте статус и логи каждого этапа

## 🛠️ Кастомизация

### Изменение триггеров:

Отредактируйте файлы `.github/workflows/*.yml`:

```yaml
on:
  push:
    branches: [ main, master, develop, feature/* ]  # Добавьте свои ветки
  pull_request:
    branches: [ main, master ]
```

### Изменение Docker Registry:

В GitHub Actions измените `REGISTRY` и `IMAGE_PREFIX`:

```yaml
env:
  REGISTRY: ghcr.io  # GitHub Container Registry
  IMAGE_PREFIX: learning-platform
```

### Добавление тестов:

Создайте файл `tests/test_services.py` и добавьте в пайплайн:

```yaml
- name: Run Python tests
  run: |
    pip install -r requirements-test.txt
    pytest tests/
```

## 📝 Версионирование

### Автоматическое версионирование:

- При push в `main/master`: используется короткий commit SHA
- При создании тега `v1.0.0`: используется версия из тега

### Создание релиза:

```bash
git tag -a v1.0.0 -m "Release version 1.0.0"
git push origin v1.0.0
```

GitHub Actions автоматически создаст релиз с собранными образами.

## 🔐 Безопасность

### Секреты:
- Никогда не коммитьте секреты в код
- Используйте GitHub Secrets или GitLab Variables
- Ротация токенов каждые 90 дней

### Сканирование:
- Trivy автоматически сканирует образы на уязвимости
- Результаты доступны в GitHub Security tab

## 🚨 Устранение проблем

### Пайплайн не запускается:
1. Проверьте, что файлы `.github/workflows/*.yml` находятся в правильной директории
2. Убедитесь, что секреты настроены правильно
3. Проверьте права доступа к репозиторию

### Ошибки сборки:
1. Проверьте логи в Actions/Pipelines
2. Убедитесь, что Dockerfile корректны
3. Проверьте зависимости в requirements.txt

### Ошибки публикации:
1. Проверьте Docker Hub credentials
2. Убедитесь, что у вас есть права на push
3. Проверьте лимиты Docker Hub (для бесплатного аккаунта)

## 📚 Дополнительные ресурсы

- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [GitLab CI/CD Documentation](https://docs.gitlab.com/ee/ci/)
- [Docker Build Push Action](https://github.com/docker/build-push-action)

