═══════════════════════════════════════════════════════════════
  КОМАНДЫ ДЛЯ ЗАПУСКА ПРИЛОЖЕНИЯ
═══════════════════════════════════════════════════════════════

🪟 WINDOWS (PowerShell) - Выполните по порядку:

1. Инициализация Docker Swarm:
   docker swarm init

2. Создание JWT секрета:
   echo jwt-secret-key-change-in-production | docker secret create jwt_secret -

3. Сборка Docker образов:
   .\build-images.bat

4. Развертывание приложения:
   docker stack deploy -c docker-compose.swarm.yml learning-platform

5. Ожидание запуска (20 секунд):
   Start-Sleep -Seconds 20

6. Инициализация тестовых данных:
   .\init-test-data.ps1

7. Открыть в браузере:
   Start-Process http://localhost:8080

═══════════════════════════════════════════════════════════════

🐧 LINUX/MAC - Выполните по порядку:

1. Инициализация Docker Swarm:
   docker swarm init

2. Создание JWT секрета:
   echo "jwt-secret-key-change-in-production" | docker secret create jwt_secret -

3. Сборка Docker образов:
   chmod +x build-images.sh
   ./build-images.sh

4. Развертывание приложения:
   docker stack deploy -c docker-compose.swarm.yml learning-platform

5. Ожидание запуска (20 секунд):
   sleep 20

6. Проверка работы:
   curl http://localhost:5000/health

7. Открыть в браузере:
   # Linux:
   xdg-open http://localhost:8080
   # Mac:
   open http://localhost:8080

═══════════════════════════════════════════════════════════════

⚡ АВТОМАТИЧЕСКАЯ УСТАНОВКА (одна команда):

Windows:
   .\install.ps1

Linux/Mac:
   chmod +x install.sh && ./install.sh

═══════════════════════════════════════════════════════════════

✅ ПРОВЕРКА РАБОТЫ:

Windows:
   .\check-app.ps1

Linux/Mac:
   curl http://localhost:5000/health
   curl http://localhost:5000/api/courses

═══════════════════════════════════════════════════════════════

🛑 ОСТАНОВКА:

   docker stack rm learning-platform

═══════════════════════════════════════════════════════════════

🔑 УЧЕТНЫЕ ДАННЫЕ:

   Преподаватель: teacher / teacher123
   Администратор: admin / admin123

═══════════════════════════════════════════════════════════════

🌐 ДОСТУП:

   Frontend: http://localhost:8080
   API: http://localhost:5000

═══════════════════════════════════════════════════════════════

